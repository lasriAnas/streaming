@AGENTS.md

## Pull Requests

Always target `claude/netflix-clone-nextjs-LBTce` as the base branch for all pull requests.

## Pushing

After every push to the feature branch, always also push the same changes to `claude/netflix-clone-nextjs-LBTce` via `mcp__github__push_files`.
